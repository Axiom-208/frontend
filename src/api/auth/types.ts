export type LogInProps = {
    email: string
    password: string
}

export type RegisterProps = {
    username: string
    email: string
    password: string
    firstName: string
    lastName: string
}